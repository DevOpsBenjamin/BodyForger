<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Depuis Android 12, le Bluetooth a ses propres permissions et ne passe plus par la
        localisation. `neverForLocation` déclare que le scan ne sert pas à situer l'utilisateur,
        ce qui évite d'exiger une permission de localisation qu'on n'utiliserait pas.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!--
        Avant Android 12, le scan BLE exigeait la localisation fine, faute de mieux : le
        système ne savait pas distinguer un scan d'appairage d'un traçage de position.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
</manifest>
