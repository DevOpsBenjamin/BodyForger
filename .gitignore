# Node / Web
web/node_modules
web/dist
node_modules
dist
.env
*.local

# macOS
.DS_Store

# Android / Gradle (for upcoming native modules)
.gradle
build/
app-mobile/build/
app-wear/build/
core-*/build/
*.apk
*.aab
.idea/
*.iml
local.properties
appscreen/

# Service account or private key: never committed, unlike google-services.json
# which is client configuration — docs/FIREBASE_SECURITY.md
*-service-account*.json
serviceAccountKey*.json
*.jks
*.keystore
*.p12
*.pem

# Fichier de confort local : lien vers AGENTS.md, qui est la source suivie
CLAUDE.md
forgefit-private
# Session handover: working state between Claude Code sessions, never shipped.
HANDOVER.md

# Local personal health & workout imports (never committed)
data/
