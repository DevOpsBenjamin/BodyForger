rules_version = '2';

// Deny by default. Read docs/FIREBASE_SECURITY.md before opening anything up.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
